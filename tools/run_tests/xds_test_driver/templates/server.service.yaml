apiVersion: v1
kind: Service
metadata:
  name: ${service_name}
  namespace: ${namespace_name}
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"${test_port}":{"name":"interop-test-psm-sec1-us-central1-grpc-test-server-java"}}}'
spec:
  type: ClusterIP
  selector:
    app: ${deployment_name}
  ports:
  - port: ${test_port}
    protocol: TCP
    targetPort: ${test_port}
